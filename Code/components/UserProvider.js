import React from 'react';

const UserProvider = ({ children }) => {
  // Your custom logic or layout can go here
  return (
    <div>
      {children}
    </div>
  );
};

export default UserProvider;
